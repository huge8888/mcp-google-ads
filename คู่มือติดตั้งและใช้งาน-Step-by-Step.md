# 📘 คู่มือติดตั้งและใช้งาน MCP Google Ads Agent แบบ Step by Step

คู่มือฉบับนี้จะพาคุณติดตั้งและใช้งาน MCP Google Ads Agent ทีละขั้นตอนแบบละเอียด **ไม่ต้องมีความรู้เรื่องการเขียนโค้ด**

---

## 📑 สารบัญ

- [ส่วนที่ 1: เตรียมความพร้อม](#ส่วนที่-1-เตรียมความพร้อม)
- [ส่วนที่ 2: ติดตั้งระบบ](#ส่วนที่-2-ติดตั้งระบบ)
- [ส่วนที่ 3: ตั้งค่า Google Ads API](#ส่วนที่-3-ตั้งค่า-google-ads-api)
- [ส่วนที่ 4: เชื่อมต่อกับ Claude](#ส่วนที่-4-เชื่อมต่อกับ-claude)
- [ส่วนที่ 5: ทดสอบการใช้งาน](#ส่วนที่-5-ทดสอบการใช้งาน)
- [ส่วนที่ 6: เริ่มใช้งานจริง](#ส่วนที่-6-เริ่มใช้งานจริง)

---

## ส่วนที่ 1: เตรียมความพร้อม

### ✅ สิ่งที่ต้องมีก่อนเริ่ม:

**1. Google Ads Account**
- มีบัญชี Google Ads (https://ads.google.com)
- มีสิทธิ์ Admin หรือ Standard access
- ถ้ายังไม่มี: สมัครที่ https://ads.google.com/home/

**2. Google Merchant Center (ถ้าขายสินค้า)**
- มีบัญชี GMC (https://merchants.google.com)
- เชื่อมต่อกับ Google Ads แล้ว
- มีสินค้าอัพโหลดแล้วอย่างน้อย 1 รายการ

**3. Claude Desktop**
- ดาวน์โหลดได้ที่: https://claude.ai/download
- ติดตั้งเสร็จแล้ว

**4. คอมพิวเตอร์**
- Windows 10/11, macOS 10.15+, หรือ Linux
- Python 3.11 หรือใหม่กว่า (จะติดตั้งในขั้นตอนถัดไป)
- อินเทอร์เน็ตเชื่อมต่อ

---

## ส่วนที่ 2: ติดตั้งระบบ

### ขั้นตอนที่ 1: ติดตั้ง Python

<details>
<summary><b>สำหรับ Windows</b> (คลิกเพื่อดูรายละเอียด)</summary>

**2.1.1 ดาวน์โหลด Python:**
1. ไปที่ https://www.python.org/downloads/
2. คลิก **"Download Python 3.11.x"** (ตัวเลขล่าสุด)
3. รอไฟล์ดาวน์โหลดเสร็จ

**2.1.2 ติดตั้ง Python:**
1. เปิดไฟล์ที่ดาวน์โหลด (python-3.11.x-amd64.exe)
2. ✅ **สำคัญ:** ติ๊กถูกที่ **"Add Python to PATH"** (ด้านล่าง)
3. คลิก **"Install Now"**
4. รอจนเสร็จ (ประมาณ 2-3 นาที)
5. คลิก **"Close"**

**2.1.3 ตรวจสอบ:**
1. กด **Windows + R**
2. พิมพ์ `cmd` แล้วกด Enter
3. พิมพ์คำสั่ง:
```bash
python --version
```
4. ควรเห็น: `Python 3.11.x`

</details>

<details>
<summary><b>สำหรับ macOS</b> (คลิกเพื่อดูรายละเอียด)</summary>

**2.1.1 ติดตั้งผ่าน Homebrew:**
1. เปิด Terminal (Applications → Utilities → Terminal)
2. ติดตั้ง Homebrew (ถ้ายังไม่มี):
```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```
3. ติดตั้ง Python:
```bash
brew install python@3.11
```

**2.1.2 ตรวจสอบ:**
```bash
python3 --version
```

</details>

<details>
<summary><b>สำหรับ Linux (Ubuntu/Debian)</b> (คลิกเพื่อดูรายละเอียด)</summary>

**2.1.1 เปิด Terminal แล้วพิมพ์:**
```bash
sudo apt update
sudo apt install python3.11 python3.11-venv python3-pip
```

**2.1.2 ตรวจสอบ:**
```bash
python3 --version
```

</details>

---

### ขั้นตอนที่ 2: ดาวน์โหลดโค้ด

**2.2.1 ติดตั้ง Git (ถ้ายังไม่มี):**

<details>
<summary><b>Windows:</b></summary>

1. ดาวน์โหลดจาก https://git-scm.com/download/win
2. ติดตั้งตามขั้นตอน (กด Next ไปเรื่อยๆ)
3. เสร็จแล้วปิด-เปิด Command Prompt ใหม่

</details>

<details>
<summary><b>macOS:</b></summary>

```bash
brew install git
```

</details>

<details>
<summary><b>Linux:</b></summary>

```bash
sudo apt install git
```

</details>

**2.2.2 Clone โปรเจค:**

เปิด Command Prompt (Windows) หรือ Terminal (Mac/Linux) แล้วพิมพ์:

```bash
# ไปที่ folder ที่ต้องการเก็บโปรเจค
cd Desktop

# Clone โปรเจค
git clone https://github.com/huge8888/mcp-google-ads.git

# เข้าไปใน folder
cd mcp-google-ads
```

---

### ขั้นตอนที่ 3: ติดตั้ง Dependencies

**2.3.1 สร้าง Virtual Environment:**

<details>
<summary><b>Windows:</b></summary>

```bash
# สร้าง virtual environment
python -m venv .venv

# เปิดใช้งาน
.venv\Scripts\activate
```

เมื่อเปิดใช้งานสำเร็จ จะเห็น `(.venv)` ด้านหน้าชื่อ path

</details>

<details>
<summary><b>macOS/Linux:</b></summary>

```bash
# สร้าง virtual environment
python3 -m venv .venv

# เปิดใช้งาน
source .venv/bin/activate
```

เมื่อเปิดใช้งานสำเร็จ จะเห็น `(.venv)` ด้านหน้าชื่อ path

</details>

**2.3.2 ติดตั้ง Python Packages:**

```bash
# อัพเกรด pip
pip install --upgrade pip

# ติดตั้ง packages ทั้งหมด
pip install -r requirements.txt
```

รอประมาณ 1-2 นาที จนเสร็จ

**2.3.3 ตรวจสอบ:**

```bash
# ตรวจสอบการติดตั้ง
python check_setup.py
```

ถ้าเห็น ✅ สีเขียวแสดงว่าสำเร็จ!

---

## ส่วนที่ 3: ตั้งค่า Google Ads API

### ขั้นตอนที่ 1: สร้าง Google Cloud Project

**3.1.1 เข้า Google Cloud Console:**
1. ไปที่ https://console.cloud.google.com/
2. Login ด้วย Google Account ที่มี access ถึง Google Ads
3. ถ้าเป็นครั้งแรก จะต้อง Accept Terms

**3.1.2 สร้าง Project ใหม่:**
1. คลิกที่ **"Select a project"** (มุมบนซ้าย)
2. คลิก **"NEW PROJECT"**
3. ตั้งชื่อ: `MCP Google Ads` (หรือชื่ออื่นที่ชอบ)
4. คลิก **"CREATE"**
5. รอประมาณ 30 วินาที
6. คลิก **"SELECT PROJECT"**

**3.1.3 เปิดใช้งาน Google Ads API:**
1. ไปที่ **"APIs & Services"** → **"Library"** (เมนูซ้าย)
2. ค้นหา: `Google Ads API`
3. คลิกที่ผลลัพธ์ **"Google Ads API"**
4. คลิก **"ENABLE"**
5. รอประมาณ 10 วินาที

---

### ขั้นตอนที่ 2: สร้าง OAuth Credentials

**3.2.1 ตั้งค่า OAuth Consent Screen:**
1. ไปที่ **"APIs & Services"** → **"OAuth consent screen"**
2. เลือก **"External"** (ถ้าไม่ใช่ Google Workspace)
3. คลิก **"CREATE"**

**กรอกข้อมูล:**
- **App name:** `MCP Google Ads Agent`
- **User support email:** อีเมลของคุณ
- **Developer contact email:** อีเมลของคุณ
- คลิก **"SAVE AND CONTINUE"**

**Scopes:**
- คลิก **"SAVE AND CONTINUE"** (ไม่ต้องเพิ่ม scopes ตอนนี้)

**Test users:**
- คลิก **"+ ADD USERS"**
- ใส่อีเมล Google Account ของคุณ
- คลิก **"ADD"**
- คลิก **"SAVE AND CONTINUE"**

**Summary:**
- คลิก **"BACK TO DASHBOARD"**

**3.2.2 สร้าง OAuth Client ID:**
1. ไปที่ **"APIs & Services"** → **"Credentials"**
2. คลิก **"+ CREATE CREDENTIALS"**
3. เลือก **"OAuth client ID"**
4. Application type: เลือก **"Desktop app"**
5. Name: `MCP Google Ads Desktop`
6. คลิก **"CREATE"**

**3.2.3 ดาวน์โหลด Credentials:**
1. จะเห็น popup "OAuth client created"
2. คลิก **"DOWNLOAD JSON"**
3. บันทึกไฟล์เป็น `client_secret.json`
4. ย้ายไฟล์ไปไว้ใน folder `mcp-google-ads`

---

### ขั้นตอนที่ 3: รับ Developer Token

**3.3.1 เข้า Google Ads:**
1. ไปที่ https://ads.google.com/
2. Login ด้วย account ที่มี Admin access

**3.3.2 ไปที่ API Center:**
1. คลิก **"Tools & Settings"** (มุมขวาบน, ไอคอนประแจ)
2. ใน **"Setup"** section → คลิก **"API Center"**

**3.3.3 รับ Developer Token:**
1. จะเห็น **"Developer token"** section
2. คลิก **"View or create new token"** (ถ้ายังไม่มี)
3. คัดลอก token (เป็นตัวอักษรและตัวเลขยาวๆ)
4. เก็บไว้ในที่ปลอดภัย (จะใช้ในขั้นตอนถัดไป)

> **หมายเหตุ:** Token อาจอยู่ในสถานะ "Pending approval" ก่อน แต่ยังใช้ทดสอบได้

---

### ขั้นตอนที่ 4: ตั้งค่า Environment Variables

**3.4.1 สร้างไฟล์ .env:**

ใน folder `mcp-google-ads` พิมพ์คำสั่ง:

```bash
# Windows:
copy .env.example .env

# Mac/Linux:
cp .env.example .env
```

**3.4.2 แก้ไขไฟล์ .env:**

เปิดไฟล์ `.env` ด้วย text editor (Notepad, VS Code, หรือ TextEdit) แล้วแก้ไข:

```bash
# Authentication Type
GOOGLE_ADS_AUTH_TYPE=oauth

# Path ไปยังไฟล์ client_secret.json (ใส่ full path)
# Windows ตัวอย่าง:
GOOGLE_ADS_CLIENT_SECRET_PATH=C:\Users\YourName\Desktop\mcp-google-ads\client_secret.json

# Mac/Linux ตัวอย่าง:
# GOOGLE_ADS_CLIENT_SECRET_PATH=/Users/yourname/Desktop/mcp-google-ads/client_secret.json

# Path สำหรับเก็บ token (ใส่ full path)
# Windows ตัวอย่าง:
GOOGLE_ADS_TOKEN_PATH=C:\Users\YourName\Desktop\mcp-google-ads\token.pickle

# Mac/Linux ตัวอย่าง:
# GOOGLE_ADS_TOKEN_PATH=/Users/yourname/Desktop/mcp-google-ads/token.pickle

# Developer Token (วางตรงนี้)
GOOGLE_ADS_DEVELOPER_TOKEN=paste-your-token-here

# Login Customer ID (ถ้ามี MCC account)
# รูปแบบ: 1234567890 (10 หลัก ไม่มีขีด)
GOOGLE_ADS_LOGIN_CUSTOMER_ID=

# Guardrails (แนะนำให้เป็น true สำหรับการทดสอบ)
DRY_RUN=true
REQUIRE_CONFIRMATION=true
MAX_BUDGET_MICROS=100000000000
MAX_CAMPAIGNS_BULK=50
```

**⚠️ สำคัญ:**
- ใช้ **full path** (path เต็ม) ไม่ใช่ relative path
- Windows ใช้ `\` หรือ `/` ก็ได้
- Mac/Linux ใช้ `/`

**3.4.3 บันทึกไฟล์**

---

### ขั้นตอนที่ 5: ทำ Authentication

**3.5.1 รัน Authentication:**

ใน Command Prompt/Terminal พิมพ์:

```bash
python google_ads_server.py
```

**3.5.2 ทำตาม OAuth Flow:**

1. **Browser จะเปิดขึ้นมาอัตโนมัติ** (ถ้าไม่เปิด ให้คัดลอก URL จาก terminal แล้ววางใน browser)

2. **เลือก Google Account** ที่มี access ถึง Google Ads

3. **จะเห็นหน้าเตือน "Google hasn't verified this app"**
   - คลิก **"Advanced"**
   - คลิก **"Go to MCP Google Ads Agent (unsafe)"**
   - ไม่ต้องกังวล! นี่คือ app ของคุณเอง

4. **หน้า Permissions:**
   - คลิก **"Allow"** (2 ครั้ง)
   - เพื่อให้ access Google Ads data

5. **เสร็จแล้ว!**
   - Browser จะแสดง: "The authentication flow has completed"
   - กลับไปดู Terminal จะเห็น: "Authentication successful!"
   - ไฟล์ `token.pickle` จะถูกสร้างขึ้น

**3.5.3 ทดสอบ:**

กด `Ctrl+C` เพื่อหยุด server ชั่วคราว

---

## ส่วนที่ 4: เชื่อมต่อกับ Claude

### ขั้นตอนที่ 1: หาไฟล์ Config ของ Claude

<details>
<summary><b>Windows</b></summary>

**4.1.1 เปิด File Explorer:**
1. กด **Windows + R**
2. พิมพ์: `%APPDATA%\Claude`
3. กด **Enter**

**4.1.2 ไฟล์ที่ต้องการ:**
- `claude_desktop_config.json`
- ถ้าไม่มี ให้สร้างไฟล์ใหม่

</details>

<details>
<summary><b>macOS</b></summary>

**4.1.1 เปิด Finder:**
1. กด **Command + Shift + G**
2. พิมพ์: `~/Library/Application Support/Claude`
3. กด **Go**

**4.1.2 ไฟล์ที่ต้องการ:**
- `claude_desktop_config.json`
- ถ้าไม่มี ให้สร้างไฟล์ใหม่

</details>

<details>
<summary><b>Linux</b></summary>

**4.1.1 เปิด Terminal:**
```bash
cd ~/.config/claude
```

**4.1.2 ไฟล์ที่ต้องการ:**
- `claude_desktop_config.json`
- ถ้าไม่มี ให้สร้างไฟล์ใหม่

</details>

---

### ขั้นตอนที่ 2: แก้ไขไฟล์ Config

**4.2.1 เปิดไฟล์ `claude_desktop_config.json` ด้วย text editor**

**4.2.2 วางโค้ดนี้ (แทนที่ทั้งหมด):**

```json
{
  "mcpServers": {
    "google-ads": {
      "command": "python",
      "args": ["ใส่-path-เต็มไปยัง/mcp-google-ads/google_ads_server.py"],
      "env": {
        "GOOGLE_ADS_CLIENT_SECRET_PATH": "ใส่-path-เต็มไปยัง/client_secret.json",
        "GOOGLE_ADS_TOKEN_PATH": "ใส่-path-เต็มไปยัง/token.pickle",
        "GOOGLE_ADS_DEVELOPER_TOKEN": "ใส่-developer-token-ของคุณ",
        "DRY_RUN": "true",
        "REQUIRE_CONFIRMATION": "true"
      }
    },
    "google-merchant-center": {
      "command": "python",
      "args": ["ใส่-path-เต็มไปยัง/mcp-google-ads/mcp-gmc/gmc_server.py"],
      "env": {
        "GOOGLE_ADS_CLIENT_SECRET_PATH": "ใส่-path-เต็มไปยัง/client_secret.json",
        "GOOGLE_ADS_TOKEN_PATH": "ใส่-path-เต็มไปยัง/token.pickle"
      }
    }
  }
}
```

**4.2.3 แทนที่ค่าต่างๆ:**

**สำหรับ Windows:**
```json
{
  "mcpServers": {
    "google-ads": {
      "command": "python",
      "args": ["C:/Users/YourName/Desktop/mcp-google-ads/google_ads_server.py"],
      "env": {
        "GOOGLE_ADS_CLIENT_SECRET_PATH": "C:/Users/YourName/Desktop/mcp-google-ads/client_secret.json",
        "GOOGLE_ADS_TOKEN_PATH": "C:/Users/YourName/Desktop/mcp-google-ads/token.pickle",
        "GOOGLE_ADS_DEVELOPER_TOKEN": "your-actual-developer-token",
        "DRY_RUN": "true",
        "REQUIRE_CONFIRMATION": "true"
      }
    },
    "google-merchant-center": {
      "command": "python",
      "args": ["C:/Users/YourName/Desktop/mcp-google-ads/mcp-gmc/gmc_server.py"],
      "env": {
        "GOOGLE_ADS_CLIENT_SECRET_PATH": "C:/Users/YourName/Desktop/mcp-google-ads/client_secret.json",
        "GOOGLE_ADS_TOKEN_PATH": "C:/Users/YourName/Desktop/mcp-google-ads/token.pickle"
      }
    }
  }
}
```

**สำหรับ macOS/Linux:**
```json
{
  "mcpServers": {
    "google-ads": {
      "command": "python3",
      "args": ["/Users/yourname/Desktop/mcp-google-ads/google_ads_server.py"],
      "env": {
        "GOOGLE_ADS_CLIENT_SECRET_PATH": "/Users/yourname/Desktop/mcp-google-ads/client_secret.json",
        "GOOGLE_ADS_TOKEN_PATH": "/Users/yourname/Desktop/mcp-google-ads/token.pickle",
        "GOOGLE_ADS_DEVELOPER_TOKEN": "your-actual-developer-token",
        "DRY_RUN": "true",
        "REQUIRE_CONFIRMATION": "true"
      }
    },
    "google-merchant-center": {
      "command": "python3",
      "args": ["/Users/yourname/Desktop/mcp-google-ads/mcp-gmc/gmc_server.py"],
      "env": {
        "GOOGLE_ADS_CLIENT_SECRET_PATH": "/Users/yourname/Desktop/mcp-google-ads/client_secret.json",
        "GOOGLE_ADS_TOKEN_PATH": "/Users/yourname/Desktop/mcp-google-ads/token.pickle"
      }
    }
  }
}
```

**⚠️ จำไว้:**
- Windows: ใช้ `"command": "python"`
- Mac/Linux: ใช้ `"command": "python3"`
- ใช้ **forward slash (/)** ในทุก platform
- ใส่ **full path** (path เต็ม)

**4.2.4 บันทึกไฟล์**

---

### ขั้นตอนที่ 3: Restart Claude Desktop

**4.3.1 ปิด Claude Desktop:**
- ปิดหน้าต่างทั้งหมด
- Windows/Linux: คลิกขวาที่ taskbar → Quit
- macOS: Command + Q

**4.3.2 เปิด Claude Desktop ใหม่:**
- เปิด Claude Desktop อีกครั้ง
- รอประมาณ 5-10 วินาที

**4.3.3 ตรวจสอบ MCP Server:**
- คลิกที่ไอคอน **🔌** (ปลั๊ก) มุมล่างซ้าย
- ควรเห็น:
  - ✅ `google-ads`
  - ✅ `google-merchant-center`
- ถ้าเห็น 2 servers นี้ แสดงว่าสำเร็จ!

---

## ส่วนที่ 5: ทดสอบการใช้งาน

### ทดสอบที่ 1: ตรวจสอบการเชื่อมต่อ

**5.1.1 เปิด Claude Desktop**

**5.1.2 พิมพ์คำสั่งทดสอบ:**

```
สวัสดี! ช่วยดูว่าเชื่อมต่อ Google Ads ได้ไหม
ลองดูรายการแคมเปญ Performance Max ทั้งหมดหน่อย
```

**5.1.3 สิ่งที่ควรเกิดขึ้น:**

Claude จะ:
1. ตรวจสอบ tools ที่มี
2. เรียกใช้ `run_gaql_query`
3. แสดงรายการแคมเปญ (ถ้ามี)

**ตัวอย่างผลลัพธ์:**
```
✅ เชื่อมต่อ Google Ads สำเร็จแล้ว!

พบ Performance Max campaigns: 3 แคมเปญ

1. Campaign: "SEW | Sunglasses PMax"
   - Status: ENABLED
   - Budget: ฿1,500/วัน

2. Campaign: "SEW | Watches PMax"
   - Status: PAUSED
   - Budget: ฿1,200/วัน

... (รายการต่อ)
```

**ถ้าเจอปัญหา:**
- ตรวจสอบไฟล์ `.env` อีกครั้ง
- ดู logs ที่ terminal
- ตรวจสอบว่า `token.pickle` มีอยู่

---

### ทดสอบที่ 2: ทดสอบ Dry-Run Mode

**5.2.1 พิมพ์คำสั่งทดสอบ:**

```
ฉันต้องการทดสอบสร้างแคมเปญใหม่

แต่อย่าทำจริงนะ แค่ dry-run

รายละเอียด:
- ชื่อ: Test Campaign
- งบ: 1000 บาท/วัน
- Target ROAS: 2.5
```

**5.2.2 สิ่งที่ควรเกิดขึ้น:**

Claude จะ:
1. ตรวจสอบว่า DRY_RUN=true
2. แสดงผลว่า "จะ" ทำอะไร
3. แต่ไม่มีการเปลี่ยนแปลงจริง

**ตัวอย่างผลลัพธ์:**
```
✅ Dry-Run Mode

สิ่งที่จะเกิดขึ้น (ถ้าทำจริง):

{
  "dry_run": true,
  "operation": "create_pmax_campaign",
  "would_execute": true,
  "params": {
    "campaign_name": "Test Campaign",
    "daily_budget_currency": 1000,
    "target_roas": 2.5
  },
  "warnings": [],
  "message": "นี่คือ DRY RUN ไม่มีการเปลี่ยนแปลงจริง"
}
```

**✅ ถ้าเห็นแบบนี้ = ระบบทำงานถูกต้อง!**

---

## ส่วนที่ 6: เริ่มใช้งานจริง

### เปลี่ยนเป็นโหมดจริง (Production Mode)

**⚠️ ก่อนเปลี่ยน:** ต้องแน่ใจว่าทดสอบใน Dry-Run จนคุ้นเคยแล้ว

**6.1 แก้ไขไฟล์ Config:**

เปิดไฟล์ `claude_desktop_config.json` แก้ไข:

```json
"DRY_RUN": "false"
```

**6.2 Restart Claude Desktop**

**6.3 ตอนนี้คำสั่งจะทำจริงแล้ว!**

---

### ตัวอย่างการใช้งานจริง

#### 📊 ตัวอย่างที่ 1: ดูรายงานประจำวัน

```
ดูผลงานเมื่อวานของแคมเปญ Performance Max ทั้งหมด

แสดง:
- ค่าใช้จ่าย
- Conversions
- Revenue
- ROAS
```

**Claude จะ:**
- ดึงข้อมูลเมื่อวาน
- คำนวณ ROAS
- เปรียบเทียบกับงบประมาณ
- แนะนำการปรับแต่ง (ถ้ามี)

---

#### 💰 ตัวอย่างที่ 2: ปรับงบประมาณ

```
แคมเปญ "SEW | Sunglasses PMax" ผลงานดีมาก
ROAS อยู่ที่ 3.2 (เป้า 2.5)

เพิ่มงบจาก 1,500 บาท เป็น 2,000 บาท/วัน
```

**Claude จะ:**
- ตรวจสอบแคมเปญ
- เปลี่ยนงบประมาณ
- ยืนยันการเปลี่ยนแปลง
- แจ้งผลที่คาดหวัง

---

#### 🏷️ ตัวอย่างที่ 3: ลดราคาสินค้า (Flash Sale)

```
ทำ flash sale แว่นกันแดด 2 วัน (10-11 พ.ย.)

ลดจาก 1,200 บาท เป็น 999 บาท
อัพเดทใน Google Merchant Center ทุกรุ่น
```

**Claude จะ:**
- ดึงรายการสินค้าแว่นกันแดดทั้งหมด
- อัพเดทราคาโปรโมชั่น
- ตั้งวันที่เริ่ม-สิ้นสุด
- แนะนำเพิ่มงบแคมเปญ (ถ้าเหมาะสม)

---

#### 📈 ตัวอย่างที่ 4: เช็ค Budget Pacing

```
ตอนนี้บ่าย 2 โมง
เช็คว่าวันนี้แคมเปญใช้งบเป็นอย่างไร
```

**Claude จะ:**
- ดึงข้อมูล spend วันนี้
- เปรียบเทียบกับงบประมาณ
- คำนวณ pacing %
- บอกว่า on track หรือ over/under pacing
- แนะนำการปรับแต่ง (ถ้าจำเป็น)

---

#### 🎯 ตัวอย่างที่ 5: สร้างแคมเปญใหม่

```
สร้างแคมเปญ Performance Max สำหรับนาฬิกา

รายละเอียด:
- ชื่อ: SEW | Watches PMax | TH | 2025-11
- งบ: 1,800 บาท/วัน
- Target ROAS: 2.8
- ใช้สินค้าที่มี custom label: watches_nov2025
- Merchant Center ID: 123456789
- วันเริ่ม: 2025-11-15
- สถานะ: หยุดไว้ก่อน (ให้ตรวจสอบก่อนเปิด)
```

**Claude จะ:**
1. สร้างแคมเปญตามที่ระบุ
2. ตั้งค่า budget และ ROAS
3. เชื่อมต่อกับ GMC
4. ใช้ feed label กรองสินค้า
5. สร้างในสถานะ PAUSED
6. แสดง summary
7. แนะนำขั้นตอนถัดไป

---

## 🎯 Tips การใช้งาน

### ✅ DO - ควรทำ

1. **ทดสอบใน Dry-Run ก่อนเสมอ**
```
ทำ [คำสั่ง] แต่ dry-run ก่อนนะ ไม่ต้องทำจริง
```

2. **ใช้คำสั่งชัดเจน**
```
❌ "เปลี่ยนงบหน่อย"
✅ "เพิ่มงบแคมเปญ 'SEW | Sunglasses PMax' จาก 1,500 เป็น 2,000 บาท/วัน"
```

3. **ขอคำอธิบาย**
```
ปรับงบตามที่แนะนำ และอธิบายว่าทำไมถึงแนะนำแบบนี้
```

4. **เก็บประวัติการเปลี่ยนแปลง**
```
ทุกครั้งที่ปรับงบให้บันทึกไว้ว่า:
- เปลี่ยนอะไร
- เมื่อไหร่
- ทำไม
- คาดว่าจะเกิดอะไร
```

### ❌ DON'T - ไม่ควรทำ

1. **อย่าใช้คำสั่งคลุมเครือ**
```
❌ "ช่วยปรับแคมเปญหน่อย"
```

2. **อย่าปรับหลายอย่างพร้อมกัน**
```
❌ เปลี่ยนงบ 5 แคมเปญพร้อมกัน (ครั้งแรก)
✅ เปลี่ยนทีละแคมเปญ ดูผลก่อน
```

3. **อย่าปิด Dry-Run เร็วเกินไป**
- ทดสอบอย่างน้อย 2-3 วัน
- คุ้นเคยกับระบบก่อน

---

## 🆘 เมื่อเจอปัญหา

### ปัญหา: Claude ไม่เห็น MCP Tools

**วิธีแก้:**
1. ตรวจสอบไฟล์ `claude_desktop_config.json`
   - Path ถูกต้องไหม (full path)
   - Syntax JSON ถูกต้องไหม
2. Restart Claude Desktop
3. ตรวจสอบว่า Python ทำงานได้ (`python --version`)

---

### ปัญหา: Authentication Failed

**วิธีแก้:**
1. ลบไฟล์ `token.pickle`
2. รัน `python google_ads_server.py` ใหม่
3. ทำ OAuth flow ใหม่

---

### ปัญหา: Permission Denied

**วิธีแก้:**
1. ตรวจสอบ Google Account มี access ถึง Google Ads
2. เช็ค Developer Token ถูกต้องไหม
3. ตรวจสอบ OAuth consent screen อนุมัติแล้ว

---

## 📞 ขอความช่วยเหลือ

**GitHub Issues:**
```
https://github.com/huge8888/mcp-google-ads/issues
```

**เอกสารเพิ่มเติม:**
- [คู่มือการใช้งาน.md](คู่มือการใช้งาน.md) - คู่มือแบบเต็ม
- [docs/USAGE_GUIDE.md](docs/USAGE_GUIDE.md) - English guide
- [docs/GAQL_RECIPES.md](docs/GAQL_RECIPES.md) - 20 query recipes

---

## ✅ Checklist ติดตั้งเสร็จสมบูรณ์

ก่อนเริ่มใช้งานจริง ให้ตรวจสอบว่าทำครบทุกข้อแล้ว:

- [ ] ✅ ติดตั้ง Python 3.11+ แล้ว
- [ ] ✅ Clone repository แล้ว
- [ ] ✅ ติดตั้ง dependencies แล้ว (`pip install -r requirements.txt`)
- [ ] ✅ สร้าง Google Cloud Project แล้ว
- [ ] ✅ เปิดใช้งาน Google Ads API แล้ว
- [ ] ✅ สร้าง OAuth credentials แล้ว
- [ ] ✅ ดาวน์โหลด `client_secret.json` แล้ว
- [ ] ✅ รับ Developer Token แล้ว
- [ ] ✅ สร้างไฟล์ `.env` และกรอกข้อมูลครบแล้ว
- [ ] ✅ ทำ authentication สำเร็จ (มีไฟล์ `token.pickle`)
- [ ] ✅ แก้ไข `claude_desktop_config.json` แล้ว
- [ ] ✅ Restart Claude Desktop แล้ว
- [ ] ✅ เห็น MCP servers ใน Claude แล้ว (🔌 icon)
- [ ] ✅ ทดสอบ dry-run สำเร็จแล้ว
- [ ] ✅ ทดสอบดึงข้อมูลแคมเปญสำเร็จแล้ว

**ถ้าครบทุกข้อ = พร้อมใช้งานแล้ว!** 🎉

---

## 🚀 เริ่มต้นใช้งาน

ตอนนี้คุณพร้อมแล้ว! ลองเริ่มต้นด้วยคำสั่งง่ายๆ เช่น:

```
สวัสดี! ช่วยดูผลงานแคมเปญ Performance Max เมื่อวานหน่อย
```

**สนุกกับการใช้งาน MCP Google Ads Agent!** 🎊

---

**เวอร์ชั่น:** 1.0
**อัพเดทล่าสุด:** 8 พฤศจิกายน 2025
**Built with ❤️ for Thai Users** 🇹🇭
